# RelayFetch

RelayFetch is a lightweight file synchronization and relay tool written in Rust. It combines file watching, content relay, and an embedded HTTP server and files can be retrieved via a proxy, making it suitable for LAN environments, development workflows, and lightweight deployments.
